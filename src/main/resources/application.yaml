spring:
  application:
    name: basketlab
  datasource:
    url: jdbc:mysql://localhost:3306/basketlab
    username: basketlab
    password: basketlab
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect